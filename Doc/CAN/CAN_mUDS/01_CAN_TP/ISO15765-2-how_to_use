
предварительно долны быть настроены message object'ы для CAN
нужно куда-то вынесены или сделать переменные, которым буду присваивать значения из дефайнов:
#define	FUNC_DIAG_REQ_CAN_ID			(0x7DF)
#define	PHY_DIAG_RESP_CAN_ID			(0x7E8)
#define	PHY_DIAG_REQ_CAN_ID				(0x7E0) 
//------------------------------------------------------------------------------------------------------------
1) 
	#include "can_tp_initialization.h"
	
	can_tp_initialization(); //внутри этой функции находятся параметры транспортного уровня(ISO 15765-2)
	
//------------------------------------------------------------------------------------------------------------
2) 
	в tp_timers.c есть функция get_tp_current_time() которая возвращает текущее время
	в неё нужно поместить нужную переменную 
	! на текущий момент всё работает при увеличении счётчика на 5мс(вставить код ниже и вызывать tick_pp() в 5 мс растре, передав аргумент 5)
	
void tick_pp(uint8 raster)
{
	tick += raster;
}

uint16 get_current_time(void)
{
	return tick;
}

//------------------------------------------------------------------------------------------------------------

3)
	засунуть в 10 мс растр
	
	#include "can_tp_iso15765_parameters.h"
	can_request_handlers();
//------------------------------------------------------------------------------------------------------------

4)
	засунуть в 10 мс растр
	
	#include "can_tp_iso15765_parameters.h"
	can_response_handlers();
//------------------------------------------------------------------------------------------------------------	
			//------------------------------------------------------------------------------------------------------------
			!!! текст пунктов 3', 4' пока не принимать во внимание, т.к. сервер не подстраивается под принятый STmin
			//------------------------------------------------------------------------------------------------------------
			3')
				засунуть в растр, соответствующий настройке STmin (параметр STmin задается в can_tp_initialization()), функцию can_request_handlers();
				поключить в этом файле: #include "can_tp_iso15765_parameters.h"
			4') 
				засунуть по возможности в часто вызываемый растр соответствующий настройке STmin (параметр STmin устанавливается клиентом) функцию can_response_handlers(),
				т.к. мы не знаем какой STmin мы примем, нужно где-то считать
				поключить хэдэр #include "can_tp_iso15765_parameters.h"
			//------------------------------------------------------------------------------------------------------------

//------------------------------------------------------------------------------------------------------------

	чтобы добавить новые CAN ID для обработки CAN TP,
	1)настройте message object для микроконтроллера
	2)добавьте в can_tp_iso15765_receive_message_segment() в switch'и обработчики ID 
	
//------------------------------------------------------------------------------------------------------------

	REQ_MSG_MAX_NUM - настроить количество одновременно обрабатываемых запросов(одновременно могут обрабатываться 
	только сообщения от разных ID)(один экземпляр занимает 4095 байт!!!!)
	
//------------------------------------------------------------------------------------------------------------

	RESP_MSG_MAX_NUM - количество одновременно отправляемых ответов(одновременно могут обрабатываться 
	только сообщения от разных ID)(один экземпляр занимает 4095 байт!!!!)
	
//------------------------------------------------------------------------------------------------------------
